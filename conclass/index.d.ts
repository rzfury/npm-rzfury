/**
 * A simple class names join function, will ignore non-string values if there's any.
 * 
 * Usually used in React projects.
 * 
 * @param {string[]} classes Class Names
 * @returns {string}
 */
export function conclass(...classes: string[]): string;
